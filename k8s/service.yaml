apiVersion: v1
kind: Service
metadata:
  name: devops-api
  namespace: devops-api
  labels:
    app.kubernetes.io/name: devops-api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: devops-api
  ports:
    - name: http
      port: 80
      targetPort: http
